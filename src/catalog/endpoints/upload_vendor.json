{
  "slug": "upload_vendor",
  "operationId": "uploadVendor",
  "method": "POST",
  "path": "/api/v1/vendors/{id}/upload",
  "name": "Uploads a vendor document",
  "category": "vendors",
  "mutating": true,
  "description": "Handles the uploading of a document to a vendor",
  "whenToUse": "Attach one or more documents/files to a vendor by id via multipart upload; use to add attachments, not to edit vendor fields (mutating).",
  "pathParameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "The Vendor Hashed ID",
      "format": "string",
      "example": "D2J234DFA"
    }
  ],
  "queryParameters": [
    {
      "name": "include",
      "type": "string",
      "required": false,
      "description": "Includes child relationships in the response, format is comma separated. Check each model for the list of associated includes",
      "example": "first_load"
    }
  ],
  "headerParameters": [],
  "requestBody": {
    "contentType": "multipart/form-data",
    "fields": [
      {
        "name": "_method",
        "type": "string",
        "required": false
      },
      {
        "name": "documents",
        "type": "array",
        "required": false
      }
    ]
  },
  "responseSchema": "Vendor"
}
