{
  "slug": "upload_recurring_invoice",
  "operationId": "uploadRecurringInvoice",
  "method": "POST",
  "path": "/api/v1/recurring_invoices/{id}/upload",
  "name": "Add recurring invoice document",
  "category": "recurring_invoices",
  "mutating": true,
  "description": "Handles the uploading of a document to a recurring_invoice",
  "whenToUse": "Attach document files to a recurring invoice by id via multipart upload; use to add supporting documents to it (mutating).",
  "pathParameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "The RecurringInvoice Hashed ID",
      "format": "string",
      "example": "D2J234DFA"
    }
  ],
  "queryParameters": [
    {
      "name": "include",
      "type": "string",
      "required": false,
      "description": "Includes child relationships in the response, format is comma separated. Check each model for the list of associated includes",
      "example": "first_load"
    }
  ],
  "headerParameters": [],
  "requestBody": {
    "contentType": "multipart/form-data",
    "fields": [
      {
        "name": "_method",
        "type": "string",
        "required": false
      },
      {
        "name": "documents",
        "type": "array",
        "required": false
      }
    ]
  },
  "responseSchema": "RecurringInvoice"
}
