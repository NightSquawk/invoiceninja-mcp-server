{
  "slug": "delete_design",
  "operationId": "deleteDesign",
  "method": "DELETE",
  "path": "/api/v1/designs/{id}",
  "name": "Deletes a design",
  "category": "designs",
  "mutating": true,
  "description": "Handles the deletion of a design by id",
  "whenToUse": "Archive/soft-delete one design by id when retiring a template you no longer want available (mutating).",
  "pathParameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "The Design Hashed ID",
      "format": "string",
      "example": "D2J234DFA"
    }
  ],
  "queryParameters": [
    {
      "name": "include",
      "type": "string",
      "required": false,
      "description": "Includes child relationships in the response, format is comma separated. Check each model for the list of associated includes",
      "example": "first_load"
    }
  ],
  "headerParameters": [],
  "requestBody": null
}
