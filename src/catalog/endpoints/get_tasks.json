{
  "slug": "get_tasks",
  "operationId": "getTasks",
  "method": "GET",
  "path": "/api/v1/tasks",
  "name": "List tasks",
  "category": "tasks",
  "mutating": false,
  "description": "Lists tasks, search and filters allow fine grained lists to be generated.\n\n  Query parameters can be added to performed more fine grained filtering of the tasks, these are handled by the TaskFilters class which defines the methods available",
  "whenToUse": "List or filter many tasks with pagination, includes, and sort; use when you need a set of task records, not one by id.",
  "pathParameters": [],
  "queryParameters": [
    {
      "name": "include",
      "type": "string",
      "required": false,
      "description": "Includes child relationships in the response, format is comma separated. Check each model for the list of associated includes",
      "example": "first_load"
    },
    {
      "name": "index",
      "type": "string",
      "required": false,
      "description": "Replaces the default response index from data to a user specific string\n\nie.\n\n```html\n  ?index=new_index\n```\n\nresponse is wrapped\n\n```json\n  {\n    'new_index' : [\n      .....\n    ]\n  }\n```",
      "example": "user"
    },
    {
      "name": "per_page",
      "type": "integer",
      "required": false,
      "description": "The number of records to return for each request, default is 20",
      "example": 20
    },
    {
      "name": "page",
      "type": "integer",
      "required": false,
      "description": "The page number to return for this request (when performing pagination), default is 1",
      "example": 1
    },
    {
      "name": "sort",
      "type": "array",
      "required": false,
      "description": "Order the list by choosing a column and direction. Each value uses the form `column|asc` or `column|desc`.\n\n*Single sort** — for example `sort=due_date|asc`.\n\n*Multiple sorts** — for example\n`sort[]=status_id|asc&sort[]=id|desc`.\n\nTasks accept `tasks` columns plus `client.name`, `client_id`, `contact.*`, and `documents` where the filter implements them. Unsupported keys are ignored.",
      "example": [
        "due_date|asc",
        "id|desc"
      ]
    }
  ],
  "headerParameters": [],
  "requestBody": null,
  "responseSchema": "Task[]"
}
