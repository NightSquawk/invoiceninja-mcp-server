{
  "slug": "update_bank_transaction_rule",
  "operationId": "updateBankTransactionRule",
  "method": "PUT",
  "path": "/api/v1/bank_transaction_rules/{id}",
  "name": "Updates a bank_transaction Rule",
  "category": "bank_transaction_rules",
  "mutating": true,
  "description": "Handles the updating of a bank_transaction rule by id",
  "whenToUse": "Modify an existing bank transaction rule by id; reach for it to change a rule's fields after creation (mutating).",
  "pathParameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "The Bank Transaction Rule Hashed ID",
      "format": "string",
      "example": "D2J234DFA"
    }
  ],
  "queryParameters": [
    {
      "name": "include",
      "type": "string",
      "required": false,
      "description": "Includes child relationships in the response, format is comma separated. Check each model for the list of associated includes",
      "example": "first_load"
    }
  ],
  "headerParameters": [],
  "requestBody": null,
  "responseSchema": "BankTransactionRule"
}
