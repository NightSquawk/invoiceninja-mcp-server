{
  "slug": "store_webhook",
  "operationId": "storeWebhook",
  "method": "POST",
  "path": "/api/v1/webhooks",
  "name": "Adds a Webhook",
  "category": "webhooks",
  "mutating": true,
  "description": "Adds an Webhook to a company",
  "whenToUse": "Create a new webhook subscription for the company when registering an endpoint to receive event callbacks (mutating).",
  "pathParameters": [],
  "queryParameters": [
    {
      "name": "include",
      "type": "string",
      "required": false,
      "description": "Includes child relationships in the response, format is comma separated. Check each model for the list of associated includes",
      "example": "first_load"
    }
  ],
  "headerParameters": [],
  "requestBody": null,
  "responseSchema": "Webhook"
}
