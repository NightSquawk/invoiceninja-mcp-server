{
  "slug": "delete_expense",
  "operationId": "deleteExpense",
  "method": "DELETE",
  "path": "/api/v1/expenses/{id}",
  "name": "Deletes a expense",
  "category": "expenses",
  "mutating": true,
  "description": "Handles the deletion of a expense by id",
  "whenToUse": "Archive/soft-delete one expense by id when removing a logged cost (mutating).",
  "pathParameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "The Expense Hashed ID",
      "format": "string",
      "example": "D2J234DFA"
    }
  ],
  "queryParameters": [
    {
      "name": "include",
      "type": "string",
      "required": false,
      "description": "Includes child relationships in the response, format is comma separated. Check each model for the list of associated includes",
      "example": "first_load"
    }
  ],
  "headerParameters": [],
  "requestBody": null
}
