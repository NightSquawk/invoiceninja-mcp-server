{
  "slug": "update_credit",
  "operationId": "updateCredit",
  "method": "PUT",
  "path": "/api/v1/credits/{id}",
  "name": "Update credit",
  "category": "credits",
  "mutating": true,
  "description": "Handles the updating of an Credit by id",
  "whenToUse": "Modify an existing credit by its hashed id; use to edit a specific credit record's fields rather than create or list credits (mutating).",
  "pathParameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "The Credit Hashed ID",
      "format": "string",
      "example": "D2J234DFA"
    }
  ],
  "queryParameters": [
    {
      "name": "include",
      "type": "string",
      "required": false,
      "description": "Includes child relationships in the response, format is comma separated. Check each model for the list of associated includes",
      "example": "first_load"
    }
  ],
  "headerParameters": [],
  "requestBody": null,
  "responseSchema": "Credit"
}
