{
  "slug": "delete_credit",
  "operationId": "deleteCredit",
  "method": "DELETE",
  "path": "/api/v1/credits/{id}",
  "name": "Delete credit",
  "category": "credits",
  "mutating": true,
  "description": "Handles the deletion of an credit by id",
  "whenToUse": "Archive/soft-delete one credit by id; reach for it to remove an existing credit (mutating).",
  "pathParameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "The Credit Hashed ID",
      "format": "string",
      "example": "D2J234DFA"
    }
  ],
  "queryParameters": [
    {
      "name": "include",
      "type": "string",
      "required": false,
      "description": "Includes child relationships in the response, format is comma separated. Check each model for the list of associated includes",
      "example": "first_load"
    }
  ],
  "headerParameters": [],
  "requestBody": null
}
