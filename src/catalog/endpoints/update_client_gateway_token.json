{
  "slug": "update_client_gateway_token",
  "operationId": "updateClientGatewayToken",
  "method": "PUT",
  "path": "/api/v1/client_gateway_tokens/{id}",
  "name": "Updates a client payment token",
  "category": "client_gateway_tokens",
  "mutating": true,
  "description": "Handles the updating of a client payment token by id",
  "whenToUse": "Modify an existing client payment gateway token by its hashed id to change its settings (mutating).",
  "pathParameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "The ClientGatewayToken Hashed ID",
      "format": "string",
      "example": "D2J234DFA"
    }
  ],
  "queryParameters": [
    {
      "name": "include",
      "type": "string",
      "required": false,
      "description": "Includes child relationships in the response, format is comma separated. Check each model for the list of associated includes",
      "example": "first_load"
    }
  ],
  "headerParameters": [],
  "requestBody": null,
  "responseSchema": "ClientGatewayToken"
}
