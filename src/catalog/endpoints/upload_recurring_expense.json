{
  "slug": "upload_recurring_expense",
  "operationId": "uploadRecurringExpense",
  "method": "POST",
  "path": "/api/v1/recurring_expenses/{id}/upload",
  "name": "Uploads a document to a recurring_expense",
  "category": "recurring_expenses",
  "mutating": true,
  "description": "Handles the uploading of a document to a recurring_expense",
  "whenToUse": "Call to attach a document file to one existing recurring expense by id via multipart upload, not to create or edit it (mutating).",
  "pathParameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "The RecurringExpense Hashed ID",
      "format": "string",
      "example": "D2J234DFA"
    }
  ],
  "queryParameters": [
    {
      "name": "include",
      "type": "string",
      "required": false,
      "description": "Includes child relationships in the response, format is comma separated. Check each model for the list of associated includes",
      "example": "first_load"
    }
  ],
  "headerParameters": [],
  "requestBody": {
    "contentType": "multipart/form-data",
    "fields": [
      {
        "name": "_method",
        "type": "string",
        "required": false
      },
      {
        "name": "documents",
        "type": "array",
        "required": false
      }
    ]
  },
  "responseSchema": "RecurringExpense"
}
