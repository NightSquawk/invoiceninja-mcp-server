{
  "slug": "upload_purchase_order",
  "operationId": "uploadPurchaseOrder",
  "method": "POST",
  "path": "/api/v1/purchase_orders/{id}/upload",
  "name": "Uploads a purchase order document",
  "category": "purchase_orders",
  "mutating": true,
  "description": "Handles the uploading of a document to a purchase_order",
  "whenToUse": "Attach a document/file to an existing purchase order by its hashed id via multipart upload (mutating).",
  "pathParameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "The Purchase Order Hashed ID",
      "format": "string",
      "example": "D2J234DFA"
    }
  ],
  "queryParameters": [
    {
      "name": "include",
      "type": "string",
      "required": false,
      "description": "Includes child relationships in the response, format is comma separated. Check each model for the list of associated includes",
      "example": "first_load"
    }
  ],
  "headerParameters": [],
  "requestBody": {
    "contentType": "multipart/form-data",
    "fields": [
      {
        "name": "_method",
        "type": "string",
        "required": false
      },
      {
        "name": "documents",
        "type": "array",
        "required": false
      }
    ]
  },
  "responseSchema": "PurchaseOrder"
}
